command /sftpsend <text> <text>:
    trigger:
        loop all players:
            if loop-player has permission "server.ops":
                send "New commit recivered (%arg-1%: %arg-2%)!" to loop-player
                broadcast "test"
                wait 1 tick
                delete {commitplayer::*}
