command /sftpsend <text> <text>:
    trigger:
        send "New commit recivered (%arg-1%: %arg-2%)!" to console, gabi_86, tnt_man268 and mixygames
        broadcast "test"
        wait 1 tick
        delete {commitplayer::*}
