command /sftpsend:
    trigger:
        loop all players:
            if loop-value is "gabi_86" or "mixigames":
                send "new commit recivered !"
        send "new commit recivered !" to console

