command /sftpsend:
    trigger:
        loop all players:
            if "%loop-player%" contains "gabi_86" or "mixigames":
                send "new commit recivered !" to loop-player
        send "new commit recivered !" to console
